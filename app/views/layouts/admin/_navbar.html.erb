<nav class="navbar navbar-expand-lg navbar-override">
  <a class="navbar-brand" href="/">ArtSwipe</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to swiper_url, class: 'nav-link' do %>
            Start Swiping <span class='sr-only'>(current)</span>
        <% end %>
      </li>
      <li class='nav-item'>
        <%= link_to new_artwork_url, class: 'nav-link' do %>
            Add Artwork <span class='sr-only'>(current)</span>
        <% end %>
      </li>
      <li class='nav-item'>
        <%= link_to artworks_url, class: 'nav-link' do %>
            View Artworks <span class='sr-only'>(current)</span>
        <% end %>
      </li>
    </ul>
    <ul class='nav navbar-nav ml-auto'>
      <%= render 'admins/profile_link' if admin_signed_in? %>
      <li class='nav-item'>
        <%= link_to_unless admin_signed_in?, 'Sign In', new_admin_session_url, { class: 'nav-link' } do %>
            <%= link_to 'Sign Out', destroy_admin_session_url, class: 'nav-link', method: :delete %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>